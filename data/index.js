const momentData = require("./moments");
const userData = require("./users");
const commentData = require("./comments");

module.exports = {
    users: userData,
    moments: momentData,
    commentData: commentData
};